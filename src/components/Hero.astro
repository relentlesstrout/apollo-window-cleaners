---
const heroImage = '/public/windowcleaning4.jpg';
---
<section class="relative w-full min-h-[60vh] lg:min-h-[80vh] flex items-start">
    <!-- Background image -->
    <div
            class="absolute inset-0 w-full h-full bg-cover bg-center filter blur-xs"
            style={`background-image: url(${heroImage});`}
    ></div>

    <!-- Optional dark overlay for readability -->
    <div class="absolute inset-0 bg-black/30"></div>

    <!-- Text overlay (top-left) -->
    <div class="relative z-10 p-6 flex flex-col gap-6 items-start">
        <h1 class="hero-line delay-1 text-white text-6xl font-extrabold">Professional</h1>
        <h1 class="hero-line delay-2 text-white text-6xl font-extrabold">Window Cleaners</h1>
        <hr class="hero-line delay-3 border-t-2 border-gray-400 opacity-50 w-full">
        <h2 class="hero-line delay-4 text-white text-4xl font-semibold">Serving Tyne Valley</h2>
        <h3 class="hero-line delay-5 text-white text-2xl font-medium">
            Window Cleaning | UPVC | Conservatories
        </h3>
        <div class="relative z-10 mt-12 w-full max-w-md">
            <a href="/Quote"
               class="hero-line delay-6 text-center py-4 px-4 hover:bg-orange-600 transition-colors duration 300 bg-orange-500 text-white text-2xl font-bold rounded-xl shadow-lg">
                Use Our Free Quote Estimator
            </a>
        </div>
    </div>

</section>

<style>
    .hero-line {
        opacity: 0;
        transform: translateX(40px);
        animation: slideFadeIn 1.2s ease-out forwards;
    }

    .delay-1 { animation-delay: 0.2s; }
    .delay-2 { animation-delay: 0.5s; }
    .delay-3 { animation-delay: 0.8s; }
    .delay-4 { animation-delay: 1.1s; }
    .delay-5 { animation-delay: 1.4s; }
    .delay-6 { animation-delay: 2.5s; }

    @keyframes slideFadeIn {
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }
</style>